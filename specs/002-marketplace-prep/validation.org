#+TITLE: W002 - marketplace preparation checks
#+STARTUP: overview
#+TODO: AMBER RED | GREEN

* Current preparation slice

** GREEN OT002.1 - isolated package check :testdef:
:PROPERTIES:
:REVISION: 76da511 plus W002 identifier and documentation changes, 2026-09-16
:EVIDENCE: Installer copied all three matching artefacts into the new ID directory; repeat installation preserved a sentinel data.json byte for byte.
:END:

Run on macOS, 2026-09-16, using a fresh mktemp directory containing
=vault with spaces/.obsidian=. Piped that vault path into =./scripts/install.sh=.
=cmp= passed for =main.js=, =manifest.json= and =styles.css=. Copied the source
manifest as a sentinel =data.json=, repeated installation, and confirmed it
unchanged with =cmp=. The parsed manifest ID was =today-note-custom-hotkey=.
No real vault was accessed. The evidence directory was created under the
operating system temporary directory and is not retained.

** GREEN OT002.2 - documentation and static review :testdef:
:PROPERTIES:
:REVISION: 76da511 plus W002 identifier and documentation changes, 2026-09-16
:EVIDENCE: README reviewed against source and installer; oxlint, bash syntax, manifest JSON checks and git diff --check passed. Documentation processed with sanitize.
:END:

The current =main.js= was inspected in full. Its entry filename and source
remain unchanged. README labels, settings persistence, command, missing-path
notice, and icon description match that source. Installer copy behaviour was
checked through OT002.1 above. The following checks succeeded:

#+begin_src sh
oxlint main.js
bash -n scripts/install.sh
jq -e '.id == "today-note-custom-hotkey" and .version == "0.1.0" and .isDesktopOnly == false' manifest.json
git diff --check
#+end_src

* Attestation workflow validation

** GREEN OT002.3 - attestation workflow checks :testdef:
:PROPERTIES:
:REVISION: 645e737 plus the W002 attestation workflow and documentation
:EVIDENCE: YAML and embedded Bash passed; isolated real Git releases incremented 1.0.1 to 1.0.2 to 1.0.3; exact asset boundary checked; invalid versions, extra assets and tagged-manifest mismatch rejected before signing.
:END:

Run on 2026-09-16 using =yq=, =jq=, Bash 5 and authenticated read-only =gh=.
Extracted the workflow's verification step into an OS temporary directory and
executed it directly. Invalid inputs =v1.0.1=, =01.0.1= and =../1.0.1= were
rejected. A shell function intercepting only =gh release view= added a synthetic
=LICENSE= asset to the real response; the actual workflow step rejected it
before attempting download. The actual =1.0.1= release inventory passed and
the workflow then rejected the known tagged-manifest mismatch via =cmp=.
JSON assertions verified manual dispatch, job permissions, the exact three
attestation subjects, and disabled storage-record creation.

Following the automatic-release decision, re-parsed the final YAML,
syntax-checked both embedded steps and checked master-only push routing and
separate job permissions. Executed the actual release Bash twice in a temporary
clone with a temporary bare remote. Real commits, annotated tags and atomic
pushes succeeded for =1.0.2= then =1.0.3=. A bounded shell substitute for
=gh release create= checked the version and exact three filenames without
creating test releases on GitHub. Files matched tag =1.0.2= byte for byte.
The fixture clone and bare remote were created under the operating system
temporary directory and are not retained.
The attestation verification script itself is unchanged from the refusal checks;
the final job now checks out the selected tag and also runs after release success.

This verifies refusal guards and structure, not successful remote signing.
=actionlint= was not installed. The live release has only the three requested
attachments. No assets or tags were changed during these checks.

* Release entry point

** GREEN OT002.4 - release entry point checks :testdef:
:PROPERTIES:
:REVISION: 0ffbcde plus the W002 release entry point working-tree change
:EVIDENCE: Both scripts pass bash -n and shellcheck; CLI contracts return the documented exits; an isolated clone with a bare remote and a substituted gh bumped 1.0.3 to 1.0.4, committed, tagged, pushed atomically and requested exactly the three files; every refusal condition exited non-zero without publishing.
:END:

Run on macOS, 2026-09-16. =bash -n= and =shellcheck= passed for
=scripts/build.sh= and =scripts/release.sh=. =make build= reported valid
artefacts at version =1.0.3=. For both scripts =--version= printed the script
version, =--help= printed the documentation file, and an unknown option exited
2.

The remaining checks ran in a =mktemp -d= fixture holding a bare remote and a
clone of this repository, with =gh= replaced on =PATH= by a recording
substitute:

- =--dry-run= reported the next version =1.0.4= and left =manifest.json= at
  =1.0.3=.
- An invalid =VERSION= value of =1.2= exited 2 without changing the repository.
- A real run wrote =1.0.4= to the manifest, committed =chore: release 1.0.4=,
  pushed the commit and annotated tag to the bare remote, and called
  =gh release create 1.0.4 main.js manifest.json styles.css --verify-tag=.
- Re-running for the existing tag exited 1.
- A dirty working tree, a branch other than =master=, and a branch ahead of its
  remote each exited 1 with the documented message.

=yj= and =jq= confirmed that the workflow triggers on a published release and
manual dispatch, exposes only the =attest= job, and grants that job
=contents: read=, =id-token: write= and =attestations: write=. The fixture was
removed with =trash=. No release, tag, or attestation was created on the real
remote.

* Limitations

- Follow-up check, 2026-09-16 :: The installer dry-run output now lists all
  three artefacts. Running =./scripts/install.sh --dry-run= against the same
  temporary vault displayed the new ID directory and all three filenames.
  =bash -n scripts/install.sh= and =git diff --check= passed afterwards.
- Commit checkpoint :: The commit attempt was rejected by an external Git
  template =commit-msg= hook outside the repository, at line 10:
  =mapfile: command not found=. The hook uses =#!/usr/bin/env bash=;
  =command -v bash= resolved to =/bin/bash= and =bash --version= reported
  =3.2.57=. No hook or environment workaround was applied. Changes remained
  staged pending resolution of that external hook environment.

- The manifest's =minAppVersion= was =1.0.0= at the time of these checks and is
  now =1.13.0=. Compatibility work remains outstanding; neither value is a
  verified support claim.
- No Obsidian host interaction or community scanner was run in this slice.
- At the preparation check, W001 host observations remained pending. All user
  tests were subsequently recorded PASS by user validation on 2026-09-16; see
  [[file:../001-today-note-access/validation.org][W001 - user validation]].
  Publication readiness remains subject to W002's remaining marketplace work.

* Licence and user acceptance update, 2026-09-16

The MIT licence was adopted and all W001 user tests were recorded PASS by user
validation. The root =LICENSE= contains the MIT terms and the README links to
it. W001's existing Back button and ribbon PASS evidence is preserved; the
three previously pending user tests now record PASS by user validation.
